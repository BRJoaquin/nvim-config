-- tokyonight. Style: "night" (darkest) | "storm" | "moon" | "day".
-- Easy to switch — change `style` below. (on_highlights/dim_inactive can be
-- added here too if you want inactive-window dimming.)
require("tokyonight").setup({
  style = "night",
})

vim.cmd.colorscheme("tokyonight")
